[phases.setup]
nixPkgs = ["python311", "pip"]

[phases.install]
cmds = [
  "pip install --upgrade pip",
  "pip uninstall python-telegram-bot -y",
  "pip install python-telegram-bot==20.8 --no-cache-dir --force-reinstall"
]

[start]
cmd = "python main.py"
